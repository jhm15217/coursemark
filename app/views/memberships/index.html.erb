<%= render "assignments/header" %>
<table>
  <tr>
    <th>Team</th>
    <th>Student</th>
    <th>Email</th>
  </tr>
  <% @memberships.sort_by{|m| m.user.first_name }.sort_by{|m| m.user.last_name }.sort_by{|m| m.team }.each do |membership| %>
      <tr>
        <td><%= membership.team %></td>
        <td><%= membership.user.name %></td>
        <td><%= membership.user.email  %></td>
      </tr>
  <% end %>
</table>
<br/>
<form method='post' action="/courses/<%= @course.id.to_s %>/assignments/<%= @assignment.id.to_s %>/memberships">
  <div class="field" >
    <textarea cols="60" name="response[teams]" rows="20"></textarea>
  </div>
  <div class="actions">
    <input name="create_group" type="submit" value="Load Team Assignments" />
  </div>
</form>
<br/>
<div>
Each line has the form [(ignored), (ignored), Student_Email, Team_First_Name, Team_Last_Name]
</div>
